const darkPalette = {
  type: "dark",
  primary: {
    main: "#FFFFFF",
    text: "#12121A",
  },
  // Add more color overrides if needed
};

const DarkTheme = {
  palette: darkPalette,
  // Add other theme configurations if needed
};

export default DarkTheme;
